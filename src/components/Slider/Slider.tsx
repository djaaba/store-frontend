import cn from 'classnames';

import styles from './Slider.module.css';
import { SliderProps } from './Slider.props';

export const Slider = ({children, ...props} : SliderProps) : JSX.Element => {

	return (
		<>
		</>
	)
};