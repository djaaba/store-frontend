import cn from 'classnames';

import styles from './HeaderBottom.module.css';
import { FallbackProps } from './Fallback.props';

export const Fallback = ({children, ...props} : FallbackProps) : JSX.Element => {

	return (
		<>
		</>
	)
};